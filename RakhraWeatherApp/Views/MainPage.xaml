<?xml version="1.0" encoding="utf-8"?>
<xamForms:ReactiveContentPage x:TypeArguments="viewModels:FavouriteWeatherViewModel" xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:d="http://xamarin.com/schemas/2014/forms/design" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                              xmlns:xamForms="clr-namespace:ReactiveUI.XamForms;assembly=ReactiveUI.XamForms"
                              xmlns:viewModels="clr-namespace:RakhraWeatherApp.ViewModels;assembly=RakhraWeatherApp"
                              mc:Ignorable="d" x:Class="RakhraWeatherApp.Views.MainPage">
    
        <!-- Place new controls here -->
        <ListView x:Name="listView" ItemsSource="{Binding Items}" HasUnevenRows="True">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout>
                            <Label Text="{Binding City}"/>
                            <Label Text="{Binding TemperatureC}"/>
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
</xamForms:ReactiveContentPage>